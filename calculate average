## a program that calculates the average of list of numbers
def calculate_average():
  numbers = list(map(float,input("Enter number separated by spaces:").split()))
  average = sum(numbers)/len(numbers)
  print(f"Average:{average:.2f}")
  
calculate_average()  
